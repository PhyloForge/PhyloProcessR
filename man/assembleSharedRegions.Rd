% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/loci.assembleSharedRegions.R
\name{assembleSharedRegions}
\alias{assembleSharedRegions}
\title{assembleSharedRegions}
\usage{
assembleSharedRegions(
  discover.directory = NULL,
  output.directory = NULL,
  min.reads.assemble = 5,
  kmer.values = c(33, 55, 77, 99, 127),
  threads = 1,
  memory = 8,
  overwrite = FALSE,
  quiet = FALSE,
  spades.path = NULL,
  samtools.path = NULL,
  bedtools.path = NULL,
  blast.path = NULL
)
}
\arguments{
\item{discover.directory}{path to the output directory from \code{discoverSharedRegions}.
Must contain \code{sample-bams/}, \code{novel_regions.bed}, and \code{novel_targets.fa}.}

\item{output.directory}{path to write per-sample contig FASTA files
(e.g. \code{data-analysis/contigs/9_genome-contigs}).}

\item{min.reads.assemble}{minimum number of reads mapping to a region for contigs
assembled to that region to be retained. Default 5.}

\item{kmer.values}{integer vector of k-mer sizes passed to SPAdes. Default
\code{c(33, 55, 77, 99, 127)}.}

\item{threads}{number of CPU threads for parallel sample processing. Default 1.}

\item{memory}{total RAM in GB available. Default 8.}

\item{overwrite}{logical. If TRUE, existing per-sample FASTAs are re-generated. Default FALSE.}

\item{quiet}{logical. If TRUE, suppresses stdout/stderr from external tools. Default FALSE.}

\item{spades.path}{path to directory containing the SPAdes executable. Default NULL
(system PATH).}

\item{samtools.path}{path to directory containing samtools executable. Default NULL
(system PATH).}

\item{bedtools.path}{path to directory containing the bedtools executable. Default NULL
(system PATH).}

\item{blast.path}{path to directory containing blastn and makeblastdb executables.
Default NULL (system PATH).}
}
\value{
Writes one FASTA file per sample to \code{output.directory}. Intermediate
working directories are written under \code{discover.directory} and cleaned up after
each sample completes. No value is returned to R.
}
\description{
Assembles per-sample contigs for each novel genomic region identified by
\code{discoverSharedRegions}. For each sample all reads overlapping any novel region are
extracted in a single BAM pass, assembled together in one SPAdes run, and the resulting
contigs are assigned to individual regions by BLASTing against the \code{novel_targets.fa}
produced by \code{discoverSharedRegions}. Only contigs assigned to a region that had
\code{min.reads.assemble} or more reads are retained. Contigs are named
\code{region_contig_N} (e.g. \code{chr3_450000_450800_contig_1}) and written as one
FASTA per sample to \code{output.directory}, compatible with the downstream
\code{filterHeterozygosity} -> \code{collectNovelContigs} -> \code{alignTargets} pipeline
(workflow X4).
}
