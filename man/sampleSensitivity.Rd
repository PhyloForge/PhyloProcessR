% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analysis.sampleSensitivity.R
\name{sampleSensitivity}
\alias{sampleSensitivity}
\title{sampleSensitivity}
\usage{
sampleSensitivity(
  alignment.directory = NULL,
  target.file = NULL,
  output.directory = "sample-sensitivity",
  threads = 1,
  memory = 1,
  mafft.path = NULL,
  overwrite = FALSE,
  quiet = FALSE
)
}
\arguments{
\item{alignment.directory}{path to a directory of phylip alignment files
(.phy), one file per marker.}

\item{target.file}{path to a FASTA file of reference/target sequences whose
names correspond to alignment file names (without the .phy extension).}

\item{output.directory}{path to the directory where output files will be
written. Default: \code{"sample-sensitivity"}.}

\item{threads}{number of parallel workers to use. Default: \code{1}.}

\item{memory}{total RAM (GB) to allocate across workers. Default: \code{1}.}

\item{mafft.path}{path to the directory containing the \code{mafft}
executable. If \code{NULL} MAFFT is expected on the system PATH.
Default: \code{NULL}.}

\item{overwrite}{logical; if \code{TRUE} the output directory is removed and
recreated. Default: \code{FALSE}.}

\item{quiet}{logical; if \code{TRUE} MAFFT screen output is suppressed.
Default: \code{FALSE}.}
}
\value{
Invisibly returns nothing. Writes
\code{sample-sensitivity_raw-data.txt} (one row per sample-marker
combination) and \code{sample-sensitivity_summary.txt} (per-sample mean,
median, min, max, and SD sensitivity) to \code{output.directory}.
}
\description{
Measures per-sample sensitivity (the proportion of each target
marker's length recovered) across a directory of phylip-format alignments.
For each marker, the alignment is re-aligned to the target reference with
MAFFT, trimmed to the reference region, and gap columns are stripped.
Sensitivity for each sample is calculated as the number of non-gap bases
divided by the target length (capped at 1). Samples absent from a marker
receive \code{NA}. Processing is parallelised across markers. Raw
per-sample per-marker values and per-sample summary statistics are saved.
}
