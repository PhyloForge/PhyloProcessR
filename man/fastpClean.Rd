% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/preprocess.fastpClean.R
\name{fastpClean}
\alias{fastpClean}
\title{fastpClean}
\usage{
fastpClean(
  input.reads = NULL,
  output.directory = NULL,
  remove.adaptors = TRUE,
  remove.duplicate.reads = TRUE,
  error.correction = TRUE,
  quality.trim.reads = FALSE,
  quality.filter = TRUE,
  low.complexity.filter = TRUE,
  trim.poly.x = TRUE,
  min.read.length = 60,
  fastp.path = NULL,
  threads = 1,
  mem = 8,
  overwrite = FALSE,
  quiet = TRUE
)
}
\arguments{
\item{input.reads}{path to a directory of raw reads. Each sample may be
organised in its own sub-directory or identified by a shared filename
prefix containing "_L00".}

\item{output.directory}{path to the directory where cleaned reads will be
saved (one sub-directory per sample).}

\item{remove.adaptors}{logical; TRUE trims adapters and detects the adapter
sequence from the read pairs (--detect_adapter_for_pe). FALSE disables
adapter trimming.}

\item{remove.duplicate.reads}{logical; TRUE removes PCR and optical
duplicate read pairs using fastp's default deduplication accuracy
(--dedup).}

\item{error.correction}{logical; TRUE corrects mismatched bases in the
overlap of a read pair by majority vote (--correction).}

\item{quality.trim.reads}{logical; TRUE trims low-quality bases from both
read ends with a sliding window (--cut_front --cut_tail). This shortens
reads and can hurt assembly, so the default is FALSE.}

\item{quality.filter}{logical; TRUE keeps the fastp quality filter, which
discards a read when more than 40 percent of its bases are below Q15.
FALSE disables the filter.}

\item{low.complexity.filter}{logical; TRUE discards low-complexity reads
below a complexity threshold of 30 percent.}

\item{trim.poly.x}{logical; TRUE trims poly-X tails, for example the poly-G
tails of two-colour Illumina chemistry.}

\item{min.read.length}{minimum read length in bp. A shorter read is
discarded. Set to 0 or NULL to disable the length filter.}

\item{fastp.path}{system path to the directory that contains the fastp
executable, or the full path to the executable; NULL searches the system
PATH.}

\item{threads}{number of CPU threads to pass to fastp.}

\item{mem}{amount of RAM in GB (currently unused by fastp directly, reserved
for future use).}

\item{overwrite}{logical; if TRUE the output directory is deleted and
recreated before processing. FALSE resumes and skips only the lanes that
already have complete output.}

\item{quiet}{logical; if TRUE fastp stdout and stderr are suppressed.}
}
\value{
invisibly returns the summary data frame; writes cleaned fastq.gz
files to output.directory, the fastp HTML and JSON reports to
logs/sample_logs, and a CSV summary to logs/fastp_summary.csv.
}
\description{
Cleans raw paired-end Illumina reads with a single pass of
fastp. The fastp command is built from the logical arguments below, so one
pass does every step that is set to TRUE. This is much faster than a
separate pass for each step, because the reads are read and written once.
Set an argument to FALSE to leave that step out of the command.
}
