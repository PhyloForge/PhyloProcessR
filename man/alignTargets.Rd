% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/alignment.alignTargets.R
\name{alignTargets}
\alias{alignTargets}
\title{alignTargets}
\usage{
alignTargets(
  targets.to.align = NULL,
  target.file = NULL,
  output.directory = "alignments",
  algorithm = c("localpair", "globalpair"),
  min.taxa = 4,
  removal.threshold = 0.35,
  subset.start = 0,
  subset.end = 1,
  adjust.direction = TRUE,
  remove.reverse.tag = TRUE,
  threads = 1,
  memory = 1,
  overwrite = FALSE,
  quiet = TRUE,
  mafft.path = NULL
)
}
\arguments{
\item{targets.to.align}{path to a FASTA file containing all sequences to be aligned,
with names in the format \code{locusName_|_sampleName}.}

\item{target.file}{path to a FASTA file of reference target sequences, one per locus,
used as a guide during alignment and for divergence filtering.}

\item{output.directory}{path to the directory where phylip alignments will be saved.
Default "alignments".}

\item{algorithm}{MAFFT alignment algorithm to use. Accepted values: "localpair" or
"globalpair". Default "localpair".}

\item{min.taxa}{threshold for the minimum number of sequences required at a locus.
Loci with this many or fewer sequences are skipped. Default 4.}

\item{removal.threshold}{maximum pairwise distance from the reference sequence allowed
for a sample sequence to be retained. Sequences exceeding this threshold are removed
before a second alignment pass. Default 0.35.}

\item{subset.start}{fractional position (0 to 1) in the sorted list of loci at which
to begin processing. Default 0.}

\item{subset.end}{fractional position (0 to 1) in the sorted list of loci at which to
stop processing. Default 1 (process all loci).}

\item{adjust.direction}{logical. If TRUE, MAFFT adjusts sequence direction before
aligning. Default TRUE.}

\item{remove.reverse.tag}{logical. If TRUE, the leading \code{_R_} tag added by MAFFT
to reverse-complemented sequences is stripped from sequence names. Default TRUE.}

\item{threads}{number of threads passed to MAFFT. Default 1.}

\item{memory}{not currently used; reserved for future parallelisation. Default 1.}

\item{overwrite}{logical. If TRUE, previously completed alignments are overwritten;
if FALSE, they are skipped. Default FALSE.}

\item{quiet}{logical. If TRUE, suppresses MAFFT screen output. Default TRUE.}

\item{mafft.path}{path to the directory containing the MAFFT executable. If NULL, MAFFT
is expected to be on the system PATH.}
}
\value{
Writes phylip alignment files to \code{output.directory}. No value is returned to R.
}
\description{
Aligns per-locus sequences extracted from a combined FASTA file using MAFFT,
guided by a reference target sequence for each locus. Sequences that diverge from the
reference by more than \code{removal.threshold} (pairwise distance) are removed and the
locus is realigned. Loci with too few sequences or with duplicate sample names are
skipped. Alignments are saved in phylip format, with the reference sequence excluded
from the final output. A fractional subset of loci can be processed for parallelisation
across jobs.
}
