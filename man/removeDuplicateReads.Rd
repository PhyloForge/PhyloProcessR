% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/preprocess.removeDuplicateReads.R
\name{removeDuplicateReads}
\alias{removeDuplicateReads}
\title{removeDuplicateReads}
\usage{
removeDuplicateReads(
  input.reads = NULL,
  output.directory = "deduped-reads",
  fastp.path = NULL,
  threads = 1,
  mem = 1,
  overwrite = FALSE,
  quiet = FALSE
)
}
\arguments{
\item{input.reads}{path to a directory of processed paired-end reads in
fastq.gz format, organised in per-sample sub-directories.}

\item{output.directory}{path to the directory where deduplicated reads will
be saved (one sub-directory per sample).}

\item{fastp.path}{system path to the directory that contains the fastp
executable, or the full path to the executable; NULL searches the system
PATH.}

\item{threads}{number of CPU threads to pass to fastp.}

\item{mem}{amount of RAM in GB (currently reserved).}

\item{overwrite}{logical; if TRUE the output directory is deleted and
recreated before processing. FALSE resumes and skips only the lanes that
already have complete output.}

\item{quiet}{logical; if TRUE fastp stdout and stderr are suppressed.}
}
\value{
invisibly returns the summary data frame; writes deduplicated
fastq.gz files to output.directory, the fastp HTML and JSON reports to
logs/sample_logs, and a CSV summary to
logs/removeDuplicateReads_summary.csv.
}
\description{
Removes PCR and optical duplicate read pairs from processed
paired-end fastq files using fastp's --dedup mode. Adapter trimming,
quality filtering, and length filtering are disabled so that only
duplicate removal is performed.
}
