% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/alignment.subsetAlignments.R
\name{makeAlignmentSubset}
\alias{makeAlignmentSubset}
\title{makeAlignmentSubset}
\usage{
makeAlignmentSubset(
  alignment.directory = NULL,
  alignment.format = "phylip",
  output.directory = NULL,
  subset.reference = c("fasta", "grep", "blast"),
  subset.fasta.file = NULL,
  subset.grep.string = NULL,
  subset.blast.targets = NULL,
  blast.path = NULL,
  quiet = TRUE,
  threads = 1,
  memory = 1,
  overwrite = FALSE
)
}
\arguments{
\item{alignment.directory}{path to the directory of input alignment files}

\item{alignment.format}{format of the input alignments; "phylip" or "fasta"}

\item{output.directory}{path to the directory where the subset alignment files will be copied}

\item{subset.reference}{method used to define the subset: "fasta" matches alignment names to sequence names in a fasta file, "grep" matches alignment file names to a pattern string, "blast" uses BLAST similarity to a set of target sequences}

\item{subset.fasta.file}{path to a fasta file whose sequence names are used to select alignments (required when subset.reference is "fasta")}

\item{subset.grep.string}{a regular expression pattern passed to grep to select alignment files by name (required when subset.reference is "grep")}

\item{subset.blast.targets}{path to a fasta file of query sequences used as BLAST queries against a database built from subset.fasta.file (required when subset.reference is "blast")}

\item{blast.path}{system path to the directory containing BLAST executables; NULL to use the system PATH}

\item{quiet}{if TRUE, suppress stdout from BLAST commands. Default TRUE.}

\item{threads}{number of CPU threads to pass to BLAST}

\item{memory}{total memory in GB (currently reserved for future use)}

\item{overwrite}{if TRUE, overwrite an existing output directory; if FALSE, keep existing files}
}
\value{
copies the selected alignment files to output.directory; nothing is returned to R
}
\description{
Selects a subset of alignments from alignment.directory and copies them to output.directory. The subset can be defined in three ways: by matching alignment file names to names in a reference fasta file ("fasta"), by grepping for a pattern in alignment file names ("grep"), or by using BLAST to match alignment names against a set of target sequences ("blast"). This allows flexible subsetting of large alignment directories by locus type or identity.
}
