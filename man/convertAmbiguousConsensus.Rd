% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/alignment.convertAmbiguousConsensus.R
\name{convertAmbiguousConsensus}
\alias{convertAmbiguousConsensus}
\title{convertAmbiguousConsensus}
\usage{
convertAmbiguousConsensus(
  alignment = NULL,
  convert.r = c("A", "G"),
  convert.y = c("C", "T"),
  convert.s = c("G", "C"),
  convert.w = c("A", "T"),
  convert.k = c("G", "T"),
  convert.m = c("A", "C"),
  convert.b = c("C", "G", "T"),
  convert.d = c("A", "G", "T"),
  convert.h = c("A", "C", "T"),
  convert.v = c("A", "C", "G")
)
}
\arguments{
\item{alignment}{a DNAStringSet (or coercible list) containing the sequences to convert.}

\item{convert.r}{replacement base for the ambiguity code R (A or G). Default c("A", "G").}

\item{convert.y}{replacement base for the ambiguity code Y (C or T). Default c("C", "T").}

\item{convert.s}{replacement base for the ambiguity code S (G or C). Default c("G", "C").}

\item{convert.w}{replacement base for the ambiguity code W (A or T). Default c("A", "T").}

\item{convert.k}{replacement base for the ambiguity code K (G or T). Default c("G", "T").}

\item{convert.m}{replacement base for the ambiguity code M (A or C). Default c("A", "C").}

\item{convert.b}{replacement base for the ambiguity code B (C, G, or T).
Default c("C", "G", "T").}

\item{convert.d}{replacement base for the ambiguity code D (A, G, or T).
Default c("A", "G", "T").}

\item{convert.h}{replacement base for the ambiguity code H (A, C, or T).
Default c("A", "C", "T").}

\item{convert.v}{replacement base for the ambiguity code V (A, C, or G).
Default c("A", "C", "G").}
}
\value{
a DNAStringSet with all specified ambiguity codes replaced by their chosen
unambiguous bases.
}
\description{
Converts IUPAC ambiguity codes in a DNAStringSet alignment to specific
unambiguous bases. Each ambiguity code has a corresponding parameter specifying which
base to substitute. This is useful for programs that do not accept ambiguous characters.
Note: parameter values supplied by the caller are currently overridden inside the
function body by fixed defaults (A or T priority).
}
