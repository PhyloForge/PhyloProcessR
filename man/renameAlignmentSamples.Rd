% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/alignment.renameAlignmentSamples.R
\name{renameAlignmentSamples}
\alias{renameAlignmentSamples}
\title{renameAlignmentSamples}
\usage{
renameAlignmentSamples(
  align.directory = NULL,
  align.extension = ".phy",
  rename.file = NULL,
  recursive = TRUE
)
}
\arguments{
\item{align.directory}{path to the directory containing alignment files to be renamed}

\item{align.extension}{file extension used to identify alignment files (e.g. ".phy")}

\item{rename.file}{path to a tab-delimited text file with a header row; must contain columns named "Old_Name" and "New_Name" specifying the current and replacement sample names}

\item{recursive}{if TRUE, search align.directory recursively to rename alignments in sub-directories as well; if FALSE, only rename alignments directly in align.directory}
}
\value{
overwrites each alignment file in place with the renamed sequences; nothing is returned to R
}
\description{
Renames sample sequences within phylip alignment files in a directory (and optionally sub-directories) using a two-column lookup table. Each alignment is read, any sample names found in the lookup table are replaced with the corresponding new name, and the file is overwritten in place.
}
