% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.sampleOccupancy.R
\name{plotOccupancy}
\alias{plotOccupancy}
\title{plotOccupancy}
\usage{
plotOccupancy(
  species.tree = NULL,
  file.directory = NULL,
  type = c("alignment", "tree"),
  out.name = "species-occupancy",
  sample.order = c("value", "alphabetical", "custom"),
  save.width = 10,
  save.height = 8,
  custom.order = NULL,
  exclude.taxa = NULL,
  proportion = TRUE,
  overwrite = FALSE
)
}
\arguments{
\item{species.tree}{path to a Newick tree file whose tip labels are the
expected sample names.}

\item{file.directory}{path to a directory of phylip alignment files (.phy).
Each file is scanned for the presence of each sample.}

\item{type}{character; type of input files. Accepted values are
\code{"alignment"} and \code{"tree"} (currently only alignment is
implemented). Default: \code{c("alignment", "tree")}.}

\item{out.name}{base name (without extension) used for the output PDF file.
Default: \code{"species-occupancy"}.}

\item{sample.order}{character; how to order samples on the x-axis. One of
\code{"value"} (ascending occupancy), \code{"alphabetical"}, or
\code{"custom"} (uses \code{custom.order}). Default:
\code{c("value", "alphabetical", "custom")}.}

\item{save.width}{width of the saved PDF in inches. Default: \code{10}.}

\item{save.height}{height of the saved PDF in inches. Default: \code{8}.}

\item{custom.order}{character vector giving the desired sample order when
\code{sample.order = "custom"}. Default: \code{NULL}.}

\item{exclude.taxa}{character vector of sample names to exclude from the
plot. Default: \code{NULL}.}

\item{proportion}{logical; if \code{TRUE} values are expressed as a
proportion of the maximum occupancy rather than raw counts. Default:
\code{TRUE}.}

\item{overwrite}{logical; reserved for future use. Default: \code{TRUE}.}
}
\value{
Returns the ggplot2 plot object invisibly. Also saves a PDF named
\code{<out.name>_occupancy-plot.pdf} in the working directory.
}
\description{
Creates a bar plot showing the number (or proportion) of
alignment files in which each sample is present. Samples are read from the
tip labels of a species tree and their presence/absence is scored across all
phylip alignment files in \code{file.directory}. The bar plot is saved as a
PDF and the ggplot2 object is returned invisibly.
}
