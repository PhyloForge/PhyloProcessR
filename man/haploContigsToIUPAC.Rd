% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/alignment.haploContigsToIUPAC.R
\name{haploContigsToIUPAC}
\alias{haploContigsToIUPAC}
\title{haploContigsToIUPAC}
\usage{
haploContigsToIUPAC(
  alignment.directory = NULL,
  alignment.format = "phylip",
  output.directory = NULL,
  output.format = "phylip",
  threads = 1,
  memory = 1,
  overwrite = FALSE,
  quiet = FALSE
)
}
\arguments{
\item{alignment.directory}{path to the directory containing haplotype alignment files.}

\item{alignment.format}{format of the input alignment files. Accepted values: "phylip"
or "fasta".}

\item{output.directory}{path to the directory where IUPAC consensus alignment files
will be saved.}

\item{output.format}{format for the output alignment files. Currently "phylip" is
supported.}

\item{threads}{number of parallel threads to use. Default 1.}

\item{memory}{total memory (in GB) to allocate across all threads. Default 1.}

\item{overwrite}{logical. If TRUE, the output directory is deleted and recreated before
processing. If FALSE and the output directory already exists, the function stops with an
error. Default FALSE.}

\item{quiet}{logical. If TRUE, suppresses per-file progress messages. Default FALSE.}
}
\value{
Writes IUPAC consensus alignment files in phylip format to
\code{output.directory}. No value is returned to R.
}
\description{
Converts phased haplotype alignments (where each taxon is represented by
two sequences distinguished by a \code{_0} / \code{_1} suffix) into single-sequence
alignments using IUPAC ambiguity codes. For each taxon, the two haplotype sequences
are merged into a consensus using \code{makeConsensus} with the IUPAC method. The
resulting alignments are saved in phylip format. Processing is parallelised across
alignment files.
}
