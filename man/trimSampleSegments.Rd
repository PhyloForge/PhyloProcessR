% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/alignment.trimSampleSegments.R
\name{trimSampleSegments}
\alias{trimSampleSegments}
\title{trimSampleSegments}
\usage{
trimSampleSegments(
  alignment = NULL,
  reference.name = NULL,
  slice.size.bp = 100,
  threshold = 0.45
)
}
\arguments{
\item{alignment}{a DNAStringSet containing the aligned sequences to process}

\item{reference.name}{name of a sequence already in the alignment to use as the reference; if NULL a majority-rule consensus is computed and used}

\item{slice.size.bp}{width of the sliding window in base pairs used to compute local pairwise distances}

\item{threshold}{pairwise distance threshold (0-1): windows with a distance from the reference at or above this value are replaced with gaps}
}
\value{
a DNAStringSet with divergent sequence segments masked as gaps and near-empty sequences removed
}
\description{
Masks divergent segments within individual sequences by sliding a window of fixed size across the alignment and replacing segments that are too divergent from a reference sequence with gaps. For each window, pairwise distance from the reference is computed for every sample; samples whose window distance exceeds the threshold have that window replaced by gaps. The reference is either a user-supplied sequence (by name) or a majority-rule consensus computed from the alignment. Samples with fewer non-gap bases than slice.size.bp after masking are removed. Alignments with three or fewer sequences are returned unmodified.
}
