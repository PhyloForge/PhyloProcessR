% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/preprocess.makeFileRename.R
\name{makeFileRename}
\alias{makeFileRename}
\title{makeFileRename}
\usage{
makeFileRename(
  read.directory = NULL,
  output.name = "file_rename.csv",
  name.samples = c("folder", "file"),
  subfolder = FALSE,
  overwrite = FALSE
)
}
\arguments{
\item{read.directory}{path to a directory containing raw fastq or fastq.gz
files, either flat or in per-sample sub-folders.}

\item{output.name}{file name (including .csv extension) for the rename table
to be written.}

\item{name.samples}{character; either "folder" to derive sample names from
the immediate parent folder of each read file, or "file" to derive sample
names by stripping read-pair suffixes (_R1, _R2, etc.) from the file name
itself.}

\item{subfolder}{logical; if TRUE and name.samples is "folder" or "file",
the leading folder component of the path is stripped before extracting the
file name, allowing one level of sub-directory nesting.}

\item{overwrite}{logical; if TRUE an existing output.name CSV is deleted
before writing.}
}
\value{
invisibly; writes a two-column CSV with columns File and Sample to
output.name.
}
\description{
Scans a directory of raw fastq files and generates a CSV
rename table mapping file names to sample names. The resulting CSV can be
supplied to organizeReads() or renameReads() to standardise file naming.
Duplicate file entries are detected and flagged as errors.
}
