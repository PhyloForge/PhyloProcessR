% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/alignment.addTaxaAlignments.R
\name{addTaxaAlignments}
\alias{addTaxaAlignments}
\title{addTaxaAlignments}
\usage{
addTaxaAlignments(
  alignment.directory = NULL,
  alignment.format = "phylip",
  output.directory = NULL,
  output.format = "phylip",
  copy.all = TRUE,
  target.markers = NULL,
  sample.markers = NULL,
  duplicate.handling = "merge",
  threads = 1,
  memory = 1,
  overwrite = FALSE,
  mafft.path = NULL
)
}
\arguments{
\item{alignment.directory}{path to the directory containing existing alignment files.}

\item{alignment.format}{format of the input alignment files. Accepted values: "phylip" or "fasta".}

\item{output.directory}{path to the directory where updated alignments will be saved.}

\item{output.format}{format for the output alignment files. Currently "phylip" is supported.}

\item{copy.all}{logical. If TRUE, alignments for which no new sequences were matched are
copied unchanged to the output directory. Default TRUE.}

\item{target.markers}{not currently used; reserved for future filtering by marker name.}

\item{sample.markers}{path to a FASTA file containing the sequences to be added. Sequence
names must follow the convention \code{locusName_|_sampleName}.}

\item{duplicate.handling}{how to resolve duplicate sample names that arise after adding
sequences. Options: "merge" (combine columns, preferring non-gap characters), "longest"
(keep the sequence with the most non-gap bases), or "original" (keep the first occurrence).
Default "merge".}

\item{threads}{number of parallel threads to use. Default 1.}

\item{memory}{total memory (in GB) to allocate across all threads. Default 1.}

\item{overwrite}{logical. If TRUE, previously completed alignments in the output directory
are overwritten; if FALSE, they are skipped. Default FALSE.}

\item{mafft.path}{path to the directory containing the MAFFT executable. If NULL, MAFFT
is expected to be on the system PATH.}
}
\value{
Writes updated alignment files in phylip format to \code{output.directory}. No
value is returned to R.
}
\description{
Adds sequences from one or more new taxa into an existing set of alignments
using MAFFT. Sequences to be added are matched to each alignment by locus name. Alignments
for which no new sequences are available can optionally be copied unchanged. Duplicate
sample names within a resulting alignment can be resolved by merging, keeping the longest
sequence, or keeping the original sequence. Processing is parallelised across alignments.
}
