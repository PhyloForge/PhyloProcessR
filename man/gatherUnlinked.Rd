% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/alignment.gatherUnlinked.R
\name{gatherUnlinked}
\alias{gatherUnlinked}
\title{gatherUnlinked}
\usage{
gatherUnlinked(
  gene.alignment.directory = NULL,
  exon.alignment.directory = NULL,
  output.directory = NULL,
  feature.gene.names = NULL,
  overwrite = FALSE
)
}
\arguments{
\item{gene.alignment.directory}{path to the directory containing gene-level concatenated
alignment files (produced by, e.g., \code{concatenateGenes}).}

\item{exon.alignment.directory}{path to the directory containing individual exon
alignment files.}

\item{output.directory}{path to the directory where the combined set of alignment files
will be saved.}

\item{feature.gene.names}{path to a tab-delimited metadata file with at minimum columns
named \code{marker} and \code{gene}, mapping each exon alignment name to a gene name.}

\item{overwrite}{logical. If TRUE, the output directory is removed and recreated before
copying; if FALSE, files are added to an existing directory. Default FALSE.}
}
\value{
Copies alignment files to \code{output.directory}. No value is returned to R.
}
\description{
Assembles a mixed set of gene-level and exon-level alignments for use in
unlinked analysis (e.g. coalescent methods). For each gene that has a concatenated gene
alignment, that file is copied to the output directory. For genes represented by only a
single exon (and therefore absent from the gene alignment directory), the corresponding
exon alignment is copied instead. The result is one alignment per locus, avoiding
redundancy between gene and exon alignments.
}
