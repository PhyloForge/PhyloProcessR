% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analysis.sampleSpecificity.R
\name{sampleSpecificity}
\alias{sampleSpecificity}
\title{sampleSpecificity}
\usage{
sampleSpecificity(
  read.directory = NULL,
  target.file = NULL,
  output.directory = "sample-specificity",
  threads = 1,
  memory = 1,
  overwrite = FALSE,
  quiet = FALSE,
  bwa.path = NULL,
  gatk4.path = NULL,
  samtools.path = NULL
)
}
\arguments{
\item{read.directory}{path to a directory where each subdirectory corresponds
to one sample and contains paired FASTQ files.}

\item{target.file}{path to a FASTA file of target marker sequences.}

\item{output.directory}{path to the directory where per-sample subdirectories
and the summary file will be written. Default: \code{"sample-specificity"}.}

\item{threads}{number of CPU threads for BWA. Default: \code{1}.}

\item{memory}{RAM in GB passed to GATK4 via \code{-Xmx}. Default: \code{1}.}

\item{overwrite}{logical; if \code{TRUE} the output directory is removed and
recreated. Default: \code{FALSE}.}

\item{quiet}{logical; if \code{TRUE} BWA and samtools screen output is
suppressed. Default: \code{FALSE}.}

\item{bwa.path}{path to the directory containing \code{bwa}. If \code{NULL}
expected on the system PATH. Default: \code{NULL}.}

\item{gatk4.path}{path to the directory containing the \code{gatk}
executable. If \code{NULL} expected on the system PATH. Default:
\code{NULL}.}

\item{samtools.path}{path to the directory containing \code{samtools}. If
\code{NULL} expected on the system PATH. Default: \code{NULL}.}
}
\value{
Invisibly returns nothing. Writes
\code{<output.directory>/sample-specificity_summary.txt} (one row per
sample) and per-sample \code{sample_specificity_raw-data.txt} files inside
each sample subdirectory.
}
\description{
Measures per-sample specificity -- the proportion of cleaned
reads that can be mapped to the target marker sequences. For each sample,
paired reads are mapped to the target sequences with BWA mem, duplicates
are marked with GATK4 MarkDuplicates, and read counts per target marker
are obtained via \code{samtools idxstats}. Specificity is computed as the
fraction of total read pairs that map to at least one target. Per-marker
raw data and an overall per-sample summary (including mapped counts,
unmapped counts, mean RPKM, and specificity) are written to the output
directory.
}
