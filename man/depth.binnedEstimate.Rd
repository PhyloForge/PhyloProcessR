% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/depth.binnedEstimate.R
\name{depth.binnedEstimate}
\alias{depth.binnedEstimate}
\title{depth.binnedEstimate}
\usage{
depth.binnedEstimate(
  depth.directory = NULL,
  sub.directory = NULL,
  output.name = "binned-depth",
  number.bins = 100,
  sample.groups = NULL,
  threads = 1,
  memory = 1,
  overwrite = FALSE
)
}
\arguments{
\item{depth.directory}{path to a directory of per-sample depth subdirectories
produced by \code{readDepth}.}

\item{sub.directory}{optional subdirectory within each sample folder that
contains the depth files. Default: \code{NULL}.}

\item{output.name}{name used for the output directory (currently not used for
output; files are written inside \code{depth.directory}). Default:
\code{"binned-depth"}.}

\item{number.bins}{number of equal-sized bins to divide each locus into.
Default: \code{100}.}

\item{sample.groups}{path to a CSV file with columns mapping samples to
groups (required but used only for group-level filtering).}

\item{threads}{number of parallel workers. Default: \code{1}.}

\item{memory}{total RAM in GB to divide across workers. Default: \code{1}.}

\item{overwrite}{logical; if \code{TRUE} an existing output directory is
deleted and recreated. Default: \code{FALSE}.}
}
\value{
Invisibly returns nothing. Writes
\code{<depth.directory>/<sample>/rpkm-binned-data.txt} for each sample, a
tab-delimited table with \code{number.bins} rows and one column per locus
containing binned RPKM values.
}
\description{
Reads per-base depth files (\code{samtools_perbase_depth.txt})
and per-locus index stats (\code{samtools_idxstats.txt}) produced by
\code{readDepth} and computes binned RPKM values for each locus. Each
locus's per-base depth data are divided into \code{number.bins} equal-sized
windows and the maximum depth within each window is normalised by locus
length and total mapped reads (RPKM). The binned RPKM table is written to
each sample's depth directory as \code{rpkm-binned-data.txt}. Processing
is parallelised across samples.
}
