% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/preprocess.fastpComplete.R
\name{fastpComplete}
\alias{fastpComplete}
\title{fastpComplete}
\usage{
fastpComplete(
  input.reads = NULL,
  output.directory = NULL,
  fastp.path = NULL,
  threads = 1,
  mem = 8,
  overwrite = FALSE,
  quiet = TRUE
)
}
\arguments{
\item{input.reads}{path to a directory of raw reads. Each sample may be
organised in its own sub-directory or identified by a shared filename
prefix containing "_L00".}

\item{output.directory}{path to the directory where cleaned reads will be
saved (one sub-directory per sample).}

\item{fastp.path}{system path to the directory containing the fastp
executable; NULL searches the system PATH.}

\item{threads}{number of CPU threads to pass to fastp.}

\item{mem}{amount of RAM in GB (currently unused by fastp directly, reserved
for future use).}

\item{overwrite}{logical; if TRUE the output directory is deleted and
recreated before processing.}

\item{quiet}{logical; if TRUE fastp stdout and stderr are suppressed.}
}
\value{
invisibly; writes cleaned fastq.gz files to output.directory and a
CSV summary to logs/fastpComplete_summary.csv.
}
\description{
Runs a comprehensive fastp cleaning step on raw paired-end
Illumina reads. In a single pass, performs adapter detection and trimming,
length filtering (minimum 60 bp), low-complexity filtering, poly-X
trimming, overlap-based base correction, and deduplication. Intended as an
all-in-one alternative to running the individual preprocess steps
separately.
}
