% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utility.runSpades.R
\name{runSpades}
\alias{runSpades}
\title{runSpades}
\usage{
runSpades(
  read.paths = NULL,
  full.path.spades = NULL,
  mismatch.corrector = TRUE,
  isolate = FALSE,
  kmer.values = c(33, 55, 77, 99, 127),
  read.contigs = F,
  save.name = NULL,
  clean = FALSE,
  threads = 1,
  memory = 4,
  overwrite = T,
  quiet = T
)
}
\arguments{
\item{read.paths}{character vector of 1, 2, or 3 paths to the input fastq.gz
read files (READ1, READ2, and optionally merged READ3).}

\item{full.path.spades}{system path to the directory containing spades.py;
NULL searches the system PATH.}

\item{mismatch.corrector}{logical; if TRUE passes --careful to SPAdes to
enable the mismatch correction module. Cannot be used with isolate = TRUE.}

\item{isolate}{logical; if TRUE passes --isolate to SPAdes, which is
optimised for high-coverage isolate data. Cannot be used with
mismatch.corrector = TRUE.}

\item{kmer.values}{integer vector of k-mer sizes to try; if the largest
k-mer causes failure it is dropped and SPAdes is rerun with the remaining
values.}

\item{read.contigs}{logical; if TRUE the assembled scaffolds (or contigs if
no scaffold file exists) are read into R and returned as a DNAStringSet.}

\item{save.name}{base name (without extension) for an output FASTA file
where the assembly is copied; NULL skips file saving.}

\item{clean}{logical; if TRUE the spades/ working directory is deleted after
assembly.}

\item{threads}{number of CPU threads to pass to SPAdes.}

\item{memory}{amount of RAM in GB to allocate to SPAdes.}

\item{overwrite}{logical; if TRUE an existing spades/ directory is deleted
before running.}

\item{quiet}{logical; if TRUE SPAdes stdout is suppressed.}
}
\value{
if read.contigs is TRUE, a DNAStringSet of assembled sequences; if
save.name is provided, a character string "Contigs were saved to file.";
otherwise "Nothing was saved.". An empty DNAStringSet is returned if no
k-mer values succeed.
}
\description{
Assembles short reads using SPAdes. Supports single-end,
paired-end, and paired-end with merged reads (1, 2, or 3 read files).
If assembly fails for the full set of k-mer values, the largest k-mer is
progressively removed and SPAdes is re-run until assembly succeeds or all
k-mer values are exhausted. The assembled scaffolds (preferred) or contigs
are optionally read back into R and/or saved to a file.
}
