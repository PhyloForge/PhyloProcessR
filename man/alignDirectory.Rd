% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/alignment.alignDirectory.R
\name{alignDirectory}
\alias{alignDirectory}
\title{alignDirectory}
\usage{
alignDirectory(
  marker.directory = NULL,
  input.format = "fasta",
  output.directory = "alignments",
  min.taxa = 4,
  subset.start = 0,
  subset.end = 1,
  adjust.direction = TRUE,
  remove.reverse.tag = TRUE,
  sample.rename = c("none", "space", "_|_"),
  threads = 1,
  memory = 1,
  overwrite = FALSE,
  quiet = TRUE,
  mafft.path = NULL
)
}
\arguments{
\item{marker.directory}{path to the directory containing unaligned FASTA files, one per locus.}

\item{input.format}{format of the input sequence files. Currently "fasta" is supported.}

\item{output.directory}{path to the directory where phylip alignments will be saved.
Default "alignments".}

\item{min.taxa}{minimum number of sequences required for a locus to be aligned. Loci
with fewer sequences are skipped. Default 4.}

\item{subset.start}{fractional position (0 to 1) in the list of loci at which to begin
processing. Useful for splitting a run across multiple jobs. Default 0.}

\item{subset.end}{fractional position (0 to 1) in the list of loci at which to stop
processing. Default 1 (process all loci).}

\item{adjust.direction}{logical. If TRUE, MAFFT adjusts sequence direction before
aligning. Default TRUE.}

\item{remove.reverse.tag}{logical. If TRUE, the leading \code{_R_} tag added by MAFFT
to reverse-complemented sequences is stripped from sequence names. Default TRUE.}

\item{sample.rename}{controls renaming of sequences before alignment. "none" leaves
names unchanged; "space" trims everything after the first space; "\emph{|}" trims everything
before and including the pipe delimiter. Default "none".}

\item{threads}{number of threads passed to MAFFT. Default 1.}

\item{memory}{not currently used; reserved for future parallelisation. Default 1.}

\item{overwrite}{logical. If TRUE, previously completed alignments are overwritten;
if FALSE, they are skipped. Default FALSE.}

\item{quiet}{logical. If TRUE, suppresses MAFFT screen output. Default TRUE.}

\item{mafft.path}{path to the directory containing the MAFFT executable. If NULL, MAFFT
is expected to be on the system PATH.}
}
\value{
Writes phylip alignment files to \code{output.directory}. No value is returned to R.
}
\description{
Aligns each FASTA file found in a directory using MAFFT (local pair
algorithm by default) and saves the resulting alignments in phylip format. Loci with
fewer than \code{min.taxa} sequences are skipped. A subset of loci can be processed
by specifying fractional start and end positions. Optionally renames sequences by
stripping everything after a pipe-delimited sample tag and removes the MAFFT reverse-
complement prefix from sequence names.
}
