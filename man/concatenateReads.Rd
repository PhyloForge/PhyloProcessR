% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/preprocess.concatenateReads.R
\name{concatenateReads}
\alias{concatenateReads}
\title{concatenateReads}
\usage{
concatenateReads(
  input.reads = NULL,
  output.directory = "processed-reads/spades-assembly",
  assembly.directory = "draft-assemblies",
  spades.path = "spades.py",
  fastqsplitter.path = "fastqsplitter",
  cap3.path = "cap3",
  number.chunks = 1,
  mismatch.corrector = FALSE,
  kmer.values = c(21, 33, 55, 77, 99, 127),
  threads = 1,
  memory = 4,
  overwrite = FALSE,
  save.all.files = FALSE,
  quiet = TRUE
)
}
\arguments{
\item{input.reads}{path to a directory of processed reads in fastq format,
expected to contain READ1, READ2, and optionally READ3 files.}

\item{output.directory}{path for the directory where chunked reads and
per-chunk SPAdes assemblies will be saved.}

\item{assembly.directory}{path for the directory where merged contig files
will be saved.}

\item{spades.path}{system path to the spades.py executable; NULL searches
the system PATH.}

\item{fastqsplitter.path}{system path to the fastqsplitter executable; NULL
searches the system PATH.}

\item{cap3.path}{system path to the CAP3 executable; defaults to \code{cap3} on
the system PATH.}

\item{number.chunks}{integer number of chunks to split the reads into before
running SPAdes on each chunk independently.}

\item{mismatch.corrector}{logical; if TRUE passes the --careful flag to
SPAdes to enable mismatch correction (slower but more accurate).}

\item{kmer.values}{integer vector of k-mer sizes to pass to SPAdes.}

\item{threads}{number of CPU threads to use for SPAdes.}

\item{memory}{amount of RAM in GB to allocate to SPAdes.}

\item{overwrite}{logical; if TRUE existing output and assembly directories
are deleted and recreated before running.}

\item{save.all.files}{logical; if FALSE intermediate per-chunk SPAdes
directories are deleted after each chunk is processed.}

\item{quiet}{logical; if TRUE, suppress output from external programs where
supported.}
}
\value{
invisibly; side effect is the production of merged contig files in
assembly.directory.
}
\description{
Concatenates multi-lane fastq reads for a sample, splits them
into chunks using fastqsplitter, assembles each chunk with SPAdes, and
iteratively merges the resulting contigs with CAP3. Designed to handle
large datasets by processing reads in parallel chunks.
}
