% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utility.setupCheck.R
\name{setupCheck}
\alias{setupCheck}
\title{setupCheck}
\usage{
setupCheck(
  anaconda.environment = "conda/PhyloCap",
  fastp.path = NULL,
  samtools.path = NULL,
  bwa.path = NULL,
  spades.path = NULL,
  bbmap.path = NULL,
  bbnorm.path = NULL,
  blast.path = NULL,
  mafft.path = NULL,
  iqtree.path = NULL,
  trimAl.path = NULL,
  julia.path = NULL,
  taper.path = NULL
)
}
\arguments{
\item{anaconda.environment}{path to a conda environment directory (e.g.
"~/miniconda3/envs/PhyloCap"). If provided, all individual path arguments
are overridden and set to anaconda.environment/bin. Set to NULL to supply
paths individually.}

\item{fastp.path}{system path to the directory containing fastp; NULL skips
this check.}

\item{samtools.path}{system path to the directory containing samtools; NULL
skips this check.}

\item{bwa.path}{system path to the directory containing bwa; NULL skips this
check.}

\item{spades.path}{system path to the directory containing spades.py; NULL
skips this check.}

\item{bbmap.path}{system path to the directory containing bbmap.sh; NULL
skips this check.}

\item{bbnorm.path}{system path to the directory containing bbnorm.sh; NULL
skips this check.}

\item{blast.path}{system path to the directory containing blastn and
makeblastdb; NULL skips this check.}

\item{mafft.path}{system path to the directory containing mafft; NULL skips
this check.}

\item{iqtree.path}{system path to the directory containing iqtree2; NULL
skips this check.}

\item{trimAl.path}{system path to the directory containing trimal; NULL skips
this check.}

\item{julia.path}{system path to the directory containing julia; NULL skips
this check (currently not checked by the function body).}

\item{taper.path}{system path to the directory containing taper; NULL skips
this check (currently not checked by the function body).}
}
\value{
logical TRUE if all checked programs were found, FALSE otherwise.
}
\description{
Verifies that the required external bioinformatics programs are
accessible on the system. Checks for fastp, samtools, bwa, spades.py,
bbmap.sh, bbnorm.sh, blastn, makeblastdb, mafft, iqtree2, and trimal. If
an anaconda.environment path is provided all tool paths are automatically
set to that environment's bin/ directory before checking. Prints a FOUND or
NOT FOUND message for each program and returns a logical indicating overall
pass/fail.
}
