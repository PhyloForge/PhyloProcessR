% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/alignment.copyUnmatchedMarkers.R
\name{copyUnmatchedMarkers}
\alias{copyUnmatchedMarkers}
\title{copyUnmatchedMarkers}
\usage{
copyUnmatchedMarkers(
  alignment.directory = NULL,
  output.directory = NULL,
  feature.gene.names = NULL,
  overwrite = FALSE
)
}
\arguments{
\item{alignment.directory}{path to the directory of per-marker alignment files to check.}

\item{output.directory}{path to the directory that unmatched alignments are added to,
typically the unlinked set produced by \code{gatherUnlinked}.}

\item{feature.gene.names}{path to a tab-delimited metadata file with at minimum columns
named \code{marker} and \code{gene}. Markers absent from its \code{marker} column are the
ones copied.}

\item{overwrite}{logical. If TRUE, an existing alignment of the same name is replaced;
if FALSE, it is left in place. Default FALSE.}
}
\value{
Copies alignment files to \code{output.directory} and invisibly returns the
number of alignments added.
}
\description{
Copies alignments whose marker name is absent from a gene-metadata file into
an existing output directory. \code{gatherUnlinked} only keeps markers listed in the gene
metadata, so markers that carry no gene assignment (for example newly discovered novel
markers) would be dropped from the unlinked set. This function adds those unmatched
markers back so every locus is represented once.
}
