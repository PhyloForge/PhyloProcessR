% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utility.runMafft.R
\name{runMafft}
\alias{runMafft}
\title{runMafft}
\usage{
runMafft(
  sequence.data = NULL,
  add.contigs = NULL,
  algorithm = c("localpair", "globalpair", "add"),
  dna.type = "nuc",
  adjust.direction = TRUE,
  save.name = NULL,
  threads = 1,
  cleanup.files = TRUE,
  mafft.path = NULL,
  quiet = TRUE
)
}
\arguments{
\item{sequence.data}{a DNAStringSet of sequences to align (or the existing
alignment when algorithm = "add").}

\item{add.contigs}{a DNAStringSet of sequences to add to the alignment when
algorithm = "add"; ignored for other algorithms.}

\item{algorithm}{character; the MAFFT alignment strategy to use. "localpair"
and "globalpair" perform a full alignment of sequence.data. "add" adds
add.contigs to sequence.data without realigning existing sequences.}

\item{dna.type}{character; sequence type passed to MAFFT (e.g. "nuc" for
nucleotide, "amino" for protein).}

\item{adjust.direction}{logical; if TRUE passes --adjustdirection to MAFFT
to automatically reverse-complement sequences that align better in the
reverse orientation.}

\item{save.name}{base name used for the temporary FASTA files written for
MAFFT; if NULL a random five-letter string is generated.}

\item{threads}{integer number of CPU threads to pass to MAFFT (used for
non-"add" algorithms).}

\item{cleanup.files}{logical; if TRUE the temporary FASTA file produced by
MAFFT is deleted after reading the alignment.}

\item{mafft.path}{system path to the directory containing the mafft
executable; NULL searches the system PATH.}

\item{quiet}{logical; if TRUE MAFFT stderr is suppressed.}
}
\value{
a DNAStringSet containing the aligned sequences.
}
\description{
Aligns sequences using MAFFT. Supports local-pair, global-pair,
and "add" modes. In "add" mode, new sequences (add.contigs) are inserted
into an existing alignment (sequence.data) without re-aligning all
sequences. All other modes perform a full multiple sequence alignment of
sequence.data. Temporary FASTA files are written and optionally cleaned up
after alignment.
}
