% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/variants.baseRecalibration.R
\name{baseRecalibration}
\alias{baseRecalibration}
\title{baseRecalibration}
\usage{
baseRecalibration(
  haplotype.caller.directory = "haplotype-caller",
  mapping.directory = "sample-mapping",
  gatk4.path = NULL,
  temp.directory = NULL,
  threads = 1,
  memory = 1,
  clean.up = TRUE,
  overwrite = FALSE,
  quiet = TRUE
)
}
\arguments{
\item{haplotype.caller.directory}{path to the directory containing per-sample
sub-directories of initial haplotype caller GVCF files (output of
haplotypeCaller()).}

\item{mapping.directory}{path to the directory containing per-sample BAM
files and reference indices (output of mapReferenceSample() or
mapReferenceConsensus()).}

\item{gatk4.path}{system path to the directory containing the gatk
executable; NULL searches the system PATH.}

\item{temp.directory}{directory for temporary recalibration files; when
NULL, the current working directory is used.}

\item{threads}{number of parallel samples to process simultaneously.}

\item{memory}{total RAM in GB to allocate; used per-thread as a JVM heap
(-Xmx).}

\item{clean.up}{logical; if TRUE intermediate VCF files generated during the
first-pass genotyping and filtering steps are deleted after the BQSR run.}

\item{overwrite}{logical; if TRUE samples that already have a BQSR GVCF are
reprocessed.}

\item{quiet}{logical; currently unused.}
}
\value{
invisibly; writes recalibrated GVCFs to haplotype.caller.directory
and recalibrated BAMs to mapping.directory.
}
\description{
Performs GATK4 base quality score recalibration (BQSR) as a
two-pass procedure. In pass 1, an initial round of genotyping and hard
filtering is run on each sample's existing haplotype caller GVCF to
produce a set of "known variants". In pass 2, GATK BaseRecalibrator and
ApplyBQSR use those variants to recalibrate the base quality scores in the
original BAM file. A final HaplotypeCaller run then produces a new GVCF
from the recalibrated BAM. Samples are processed in parallel.
}
