% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/preprocess.readErrorCorrection.R
\name{readErrorCorrection}
\alias{readErrorCorrection}
\title{readErrorCorrection}
\usage{
readErrorCorrection(
  input.reads = "adaptor-removed-reads",
  output.directory = "corrected-reads",
  fastp.path = NULL,
  threads = 1,
  mem = 1,
  overwrite = FALSE,
  quiet = TRUE
)
}
\arguments{
\item{input.reads}{path to a directory of processed paired-end reads in
fastq.gz format, organised in per-sample sub-directories.}

\item{output.directory}{path to the directory where error-corrected reads
will be saved (one sub-directory per sample).}

\item{fastp.path}{system path to the directory containing the fastp
executable; NULL searches the system PATH.}

\item{threads}{number of CPU threads to pass to fastp.}

\item{mem}{amount of RAM in GB (currently reserved).}

\item{overwrite}{logical; if TRUE the output directory is deleted and
recreated before processing.}

\item{quiet}{logical; if TRUE fastp stdout and stderr are suppressed.}
}
\value{
invisibly; writes error-corrected fastq.gz files to output.directory
and a CSV summary to logs/readErrorCorrection_summary.csv.
}
\description{
Performs overlap-based read error correction on paired-end
fastq reads using fastp's --correction flag. Only the correction step is
applied; adapter trimming, quality filtering, and length filtering are all
disabled. fastp corrects mismatches in the overlapping region of paired
reads by majority vote.
}
