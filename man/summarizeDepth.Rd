% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/depth.markerSummary.R
\name{summarizeDepth}
\alias{summarizeDepth}
\title{summarizeDepth}
\usage{
summarizeDepth(
  depth.directory = NULL,
  sub.directory = NULL,
  output.directory = "depth-summary",
  sample.groups = NULL,
  remove.samples = NULL,
  threads = 1,
  mem = 1,
  overwrite = FALSE
)
}
\arguments{
\item{depth.directory}{path to a directory of per-sample depth subdirectories
produced by \code{readDepth}, each containing
\code{species_summary_data.txt}.}

\item{sub.directory}{optional subdirectory within each sample folder that
contains the summary file. Default: \code{NULL}.}

\item{output.directory}{path to the directory where summary CSVs and PDF
plots will be written. Default: \code{"depth-summary"}.}

\item{sample.groups}{path to a CSV file with columns \code{Group} and
\code{Sample} (or similar two-column layout) mapping samples to groups.}

\item{remove.samples}{character vector of sample names to exclude from the
analysis. Default: \code{NULL}.}

\item{threads}{not currently used. Default: \code{1}.}

\item{mem}{not currently used. Default: \code{1}.}

\item{overwrite}{logical; if \code{TRUE} the output directory is deleted and
recreated. Default: \code{FALSE}.}
}
\value{
Invisibly returns nothing. Writes \code{sample_rpkm-summary.csv}
(per-sample RPKM statistics), \code{marker_rpkm-summary.csv} (per-marker
RPKM statistics by group), and several PDF comparison plots to
\code{output.directory}.
}
\description{
Aggregates per-sample locus depth data produced by
\code{readDepth} into group-level and marker-level RPKM summaries.
For each sample, locus RPKM values are summarised (mean, median, SD, min,
max). Across samples, marker-level RPKM statistics are computed per group.
Group-level dot plots and box plots of sample and marker RPKM are saved as
PDFs using ggplot2.
}
