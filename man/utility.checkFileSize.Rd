% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utility.checkFileSize.R
\name{utility.checkFileSize}
\alias{utility.checkFileSize}
\title{utility.checkFileSize}
\usage{
utility.checkFileSize(
  read.directory = NULL,
  check.file.name = NULL,
  output.name = "check-fileSize",
  overwrite = FALSE
)
}
\arguments{
\item{read.directory}{path to the directory containing the fastq.gz read
files to be checked.}

\item{check.file.name}{path to a two-column whitespace-delimited text file
where column 1 is the expected file size in bytes and column 2 is the file
name. Typically produced by running \code{ls -l} or a similar command on
the source server.}

\item{output.name}{base name (without extension) for the output CSV
containing PASS/FAIL results.}

\item{overwrite}{logical; if TRUE an existing output CSV is deleted before
writing.}
}
\value{
a data frame with columns File_Name, Read_fileSize (measured),
Read_checkSize (expected), and checkSize_Pass (PASS or FAIL). Also written
to output.name.csv.
}
\description{
Verifies the integrity of raw read files by comparing each
file's actual size on disk against expected sizes recorded in a reference
size file. Produces a PASS/FAIL report per file and writes the results to
a CSV.
}
