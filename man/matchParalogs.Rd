% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/alignment.matchParalogs.R
\name{matchParalogs}
\alias{matchParalogs}
\title{matchParalogs}
\usage{
matchParalogs(
  assembly.directory = NULL,
  target.file = NULL,
  alignment.contig.name = NULL,
  output.directory = "match-targets",
  min.match.percent = 50,
  min.match.length = 40,
  min.match.coverage = 50,
  trim.target = FALSE,
  threads = 1,
  memory = 1,
  overwrite = FALSE,
  resume = TRUE,
  quiet = TRUE,
  blast.path = NULL,
  bbmap.path = NULL
)
}
\arguments{
\item{assembly.directory}{path to a directory of per-sample assembly fasta files (one file per sample)}

\item{target.file}{path to a fasta file of paralog target sequences to match against each assembly}

\item{alignment.contig.name}{label prefix used for naming the output contig alignment files}

\item{output.directory}{path to the directory where per-sample paralog output files will be saved}

\item{min.match.percent}{minimum BLAST percent identity required to retain a contig match}

\item{min.match.length}{minimum number of matching base pairs required to retain a BLAST hit}

\item{min.match.coverage}{minimum percent of the query length that must be covered by a BLAST hit}

\item{trim.target}{if TRUE, trim target sequences before matching (currently unused placeholder)}

\item{threads}{number of CPU threads to pass to BLAST}

\item{memory}{total memory in GB (currently unused, reserved for future use)}

\item{overwrite}{if TRUE, redo and overwrite samples already completed; if FALSE, skip them}

\item{resume}{if TRUE, skip samples whose output files already exist}

\item{quiet}{if TRUE, suppress BLAST screen output}

\item{blast.path}{system path to the directory containing BLAST executables; NULL to use the system PATH}

\item{bbmap.path}{system path to the directory containing BBMap executables; NULL to use the system PATH}
}
\value{
saves per-sample fasta files of matching paralog contigs to output.directory; nothing is returned to R
}
\description{
For each sample assembly in assembly.directory, uses BLAST (dc-megablast) to identify contigs that match a provided set of paralog target sequences. Matching contigs are extracted and saved per sample as a fasta file of candidate paralog sequences, enabling downstream paralog detection and filtering.
}
