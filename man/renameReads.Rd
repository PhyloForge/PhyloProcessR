% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/preprocess.renameReads.R
\name{renameReads}
\alias{renameReads}
\title{renameReads}
\usage{
renameReads(
  sample.spreadsheet = NULL,
  output.directory = NULL,
  read.directory = ".",
  skip.not.found = FALSE,
  lane.names = NULL,
  overwrite = FALSE
)
}
\arguments{
\item{sample.spreadsheet}{path to a CSV file with columns Old_Name and
New_Name specifying the renaming mapping.}

\item{output.directory}{path to the directory where renamed files will be
copied.}

\item{read.directory}{path to the directory containing the raw fastq.gz
files. Defaults to the current working directory.}

\item{skip.not.found}{logical; if TRUE samples in the spreadsheet whose
files cannot be located are silently skipped rather than raising an error.}

\item{lane.names}{character vector of lane label substrings (e.g. c("s2",
"s3")) used to distinguish reads from different lanes when a sample has
multiple lanes; NULL assumes a single lane per sample.}

\item{overwrite}{logical; if TRUE the output directory is deleted and
recreated before copying files.}
}
\value{
invisibly; side effect is renamed fastq.gz files written to
output.directory.
}
\description{
Renames and copies raw fastq.gz read files to a new directory
using a sample spreadsheet that maps old names to new names. Supports
samples spread across multiple lanes by specifying lane label strings via
lane.names. Output files are named using the convention
NewName_L00N_READ1/2.fastq.gz.
}
