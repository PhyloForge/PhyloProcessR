% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analysis.geneFamilyStats.R
\name{geneFamilyStats}
\alias{geneFamilyStats}
\title{geneFamilyStats}
\usage{
geneFamilyStats(
  assembly.directory = NULL,
  target.file = NULL,
  gene.family.file = NULL,
  output.directory = "gene-family-stats",
  min.match.percent = 50,
  min.match.length = 60,
  min.match.coverage = 50,
  max.match.copy = 100,
  threads = 1,
  memory = 1,
  overwrite = FALSE,
  quiet = TRUE,
  blast.path = NULL
)
}
\arguments{
\item{assembly.directory}{path to a directory of assembly FASTA files (.fa),
one file per sample.}

\item{target.file}{path to a FASTA file of target sequences to BLAST against
the assemblies.}

\item{gene.family.file}{path to a CSV file with columns \code{target_name}
and \code{gene_family} that maps each target to a gene-family label.}

\item{output.directory}{path to the directory where per-sample subdirectories
and summary files will be written. Default: \code{"gene-family-stats"}.}

\item{min.match.percent}{minimum BLAST percent identity required to retain a
hit. Default: \code{50}.}

\item{min.match.length}{minimum alignment length (bp) required to retain a
hit. Default: \code{60}.}

\item{min.match.coverage}{minimum percentage of the query length that must be
covered by the alignment to retain a hit. Default: \code{50}.}

\item{max.match.copy}{maximum number of contig hits allowed per target locus
before the locus is discarded. Default: \code{100}.}

\item{threads}{number of CPU threads to pass to BLAST. Default: \code{1}.}

\item{memory}{not currently used; reserved for future use. Default: \code{1}.}

\item{overwrite}{logical; if \code{TRUE} existing per-sample directories are
removed and recomputed. Default: \code{FALSE}.}

\item{quiet}{logical; if \code{TRUE} BLAST screen output is suppressed.
Default: \code{TRUE}.}

\item{blast.path}{path to the directory containing BLAST executables
(\code{makeblastdb}, \code{blastn}). If \code{NULL} the executables are
expected on the system PATH. Default: \code{NULL}.}
}
\value{
Invisibly returns nothing. Writes three summary files to
\code{output.directory}: \code{sample_summary.txt} (one row per sample),
\code{gene-family_summary_short.txt} (copy counts per family per sample),
and \code{gene-family_summary_long.txt} (detailed per-sample per-family
statistics). Individual per-sample files are written to subdirectories.
}
\description{
Uses BLAST to match a set of target sequences to sample
assemblies and summarises copy-number statistics broken down by gene family.
For each sample, contigs are renamed, a BLAST database is built, and target
sequences are queried against it. Hits are filtered by percent identity,
match length, and coverage. Loci with more than \code{max.match.copy} hits
are excluded. Per-sample and per-gene-family summary tables are written to
the output directory.
}
