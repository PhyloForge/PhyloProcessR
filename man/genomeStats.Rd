% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analysis.genomeStats.R
\name{genomeStats}
\alias{genomeStats}
\title{genomeStats}
\usage{
genomeStats(
  genome.directory = NULL,
  output.directory = "alignments",
  threads = 1,
  memory = 1,
  overwrite = FALSE,
  resume = TRUE,
  quiet = TRUE
)
}
\arguments{
\item{genome.directory}{path to a directory of genome FASTA files, one file
per assembly.}

\item{output.directory}{path to the directory where the output CSV will be
written. Default: \code{"alignments"}.}

\item{threads}{number of CPU threads (not currently used). Default: \code{1}.}

\item{memory}{RAM in GB (not currently used). Default: \code{1}.}

\item{overwrite}{logical; if \code{TRUE} the output directory is deleted and
recreated. Default: \code{FALSE}.}

\item{resume}{logical; if \code{TRUE} already-completed samples are skipped.
Cannot be \code{TRUE} when \code{overwrite} is \code{TRUE}. Default:
\code{TRUE}.}

\item{quiet}{logical; reserved for future use. Default: \code{TRUE}.}
}
\value{
Invisibly returns nothing. Writes
\code{<output.directory>/Spades_assembly_stats.csv} with one row per
genome assembly.
}
\description{
Computes assembly quality statistics for every genome FASTA file
in a directory. Reported metrics include: total assembly size in gigabases,
number of contigs/scaffolds, mean and median contig length, maximum and
minimum contig length, N50, L50, N90, L90, and contig counts above
size thresholds of 1 KB, 10 KB, 100 KB, 500 KB, 1 MB, 10 MB, and 100 MB.
All results are written to a single CSV file.
}
